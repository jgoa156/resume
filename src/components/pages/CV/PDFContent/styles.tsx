import { StyleSheet } from '@react-pdf/renderer';

const styles = StyleSheet.create({
  page: {
    display: "flex",
    height: "100%",
    flexDirection: "row",
    fontSize: "10px",
  },
});

export default styles;