import { StyleSheet } from '@react-pdf/renderer';

const styles = StyleSheet.create({
  bioParagraph: {
    color: "white",
    fontSize: "10px",
    marginBottom: "10px",
  },
});

export default styles;