import { StyleSheet } from '@react-pdf/renderer';

const styles = StyleSheet.create({
  title: {
    marginBottom: 5,

    color: "white",
    fontSize: "24px",
    fontFamily: "Parkson",
    whiteSpace: "nowrap",
    overflow: "hidden",
  },
  border: {
    marginBottom: 5
  }
});

export default styles;